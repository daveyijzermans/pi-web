[Unit]
Description=pi-web — browser-based pi session viewer and chat
Documentation=https://github.com/ygncode/pi-web
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
ExecStart=/usr/local/bin/pi-web
Restart=on-failure
RestartSec=5

# Kill only the server on stop/restart: detached worker-holders (and the pi
# agents they hold) live in this cgroup and must survive server restarts.
KillMode=process

# Pass PI_WEB_TOKEN from the environment if set
EnvironmentFile=-%h/.config/pi-web/env

[Install]
WantedBy=default.target
